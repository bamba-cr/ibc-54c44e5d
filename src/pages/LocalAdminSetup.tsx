
import { LocalAdminSetup } from '@/components/admin/LocalAdminSetup';

const LocalAdminSetupPage = () => {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gradient-to-br from-primary-dark to-primary p-4">
      <LocalAdminSetup />
    </div>
  );
};

export default LocalAdminSetupPage;
